# Telegram bot token used to validate init data HMAC signatures
BOT_TOKEN=

# Supabase project credentials (service role required for server-side RLS operations)
SUPABASE_URL=
SUPABASE_SERVICE_KEY=

# Path segment for the Telegram webhook route (see app/api/tg/webhook/[secret])
WEBHOOK_SECRET_PATH=

# Dictionary storage configuration (Supabase Storage bucket + optional CDN URL)
DICTIONARY_BUCKET=game-dictionary
DICTIONARY_URL=
